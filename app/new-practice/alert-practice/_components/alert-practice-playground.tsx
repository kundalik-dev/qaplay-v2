import styles from "./alert-practice-playground.module.css";

/**
 * Alert Practice — custom practice UI.
 *
 * This is YOUR canvas. Replace the markup below with your own HTML/CSS for the
 * alert/confirm/prompt practice element. It renders inside <PracticeView> in
 * the Practice tab. Keep stable data-testid hooks for automation.
 */
export function AlertPracticePlayground() {
  return (
    <div className={styles.root} data-testid="alert-practice-playground">
      {/* TODO: build the custom alert/confirm/prompt UI here. */}
    </div>
  );
}
