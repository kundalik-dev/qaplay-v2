import { createPageMetadata } from "../create-page-metadata";

export const annotationsPageMetadata = createPageMetadata({
  title: "How to Use Test Annotations in Playwright and Selenium",
  description:
    "Practice Playwright test.skip(), test.slow(), test.fixme(), test.fail(), test.step(), and test.each() — plus TestNG @DataProvider and @Test equivalents — in realistic interactive scenarios.",
  path: "/practice/annotations",
  ogType: "article",
  keywords: [
    "playwright test.skip",
    "playwright test.fixme",
    "playwright test.slow",
    "playwright test.fail",
    "playwright test.step",
    "playwright test.each",
    "testng annotations",
    "testng data provider",
    "selenium skip test",
    "playwright annotations tutorial",
    "test annotations automation",
    "parameterized tests playwright",
  ],
});
