import type { TestCase } from "@/data/practice-data/types";

export const dynamicWaitsTestCases: TestCase[] = [
  {
    id: "DW_001",
    scenario: "Verify waitForSelector resolves when delayed element appears",
    expected: "Element is visible within the wait timeout.",
    type: "positive",
    priority: "high",
    steps: [
      'Click <code>[data-testid="dw-trigger-delayed"]</code>.',
      'Call <code>waitForSelector(\'[data-testid="dw-delayed-result"]\', { state: "visible" })</code>.',
      'Assert the element text is not empty.',
    ],
  },
  {
    id: "DW_002",
    scenario: "Verify waitForSelector times out when element never appears",
    expected: "A TimeoutError is thrown after the configured timeout.",
    type: "negative",
    priority: "high",
    steps: [
      'Do NOT click the trigger.',
      'Call <code>waitForSelector(\'[data-testid="dw-delayed-result"]\', { timeout: 1000 })</code>.',
      'Assert a <code>TimeoutError</code> is thrown.',
    ],
  },
  {
    id: "DW_003",
    scenario: "Verify spinner appears immediately after trigger click",
    expected: "dw-spinner is visible before the data loads.",
    type: "positive",
    priority: "medium",
    steps: [
      'Click <code>[data-testid="dw-trigger-spinner"]</code>.',
      'Immediately assert <code>[data-testid="dw-spinner"]</code> is visible.',
    ],
  },
  {
    id: "DW_004",
    scenario: "Verify spinner disappears and content appears after loading",
    expected: "dw-spinner hidden; dw-spinner-content visible.",
    type: "positive",
    priority: "high",
    steps: [
      'Click <code>[data-testid="dw-trigger-spinner"]</code>.',
      'Wait for <code>[data-testid="dw-spinner"]</code> to have state <code>"hidden"</code>.',
      'Assert <code>[data-testid="dw-spinner-content"]</code> is visible.',
    ],
  },
  {
    id: "DW_005",
    scenario: "Verify toast message appears and contains expected text",
    expected: "dw-toast is visible with correct message text.",
    type: "positive",
    priority: "high",
    steps: [
      'Click <code>[data-testid="dw-trigger-toast"]</code>.',
      'Wait for <code>[data-testid="dw-toast"]</code> to be visible.',
      'Assert its text contains "Success".',
    ],
  },
  {
    id: "DW_006",
    scenario: "Verify toast auto-dismisses within 4 seconds",
    expected: "dw-toast is no longer in DOM after dismiss delay.",
    type: "positive",
    priority: "medium",
    steps: [
      'Click the toast trigger.',
      'Assert the toast appears.',
      'Wait 4 seconds.',
      'Assert <code>[data-testid="dw-toast"]</code> has state <code>"hidden"</code> or does not exist.',
    ],
  },
  {
    id: "DW_007",
    scenario: "Verify waitForFunction resolves when counter reaches target",
    expected: "waitForFunction returns when dw-poll-count text equals '5'.",
    type: "positive",
    priority: "high",
    steps: [
      'Click <code>[data-testid="dw-trigger-poll"]</code>.',
      'Call <code>page.waitForFunction(() => document.querySelector(\'[data-testid="dw-poll-count"]\')?.textContent === "5")</code>.',
      'Assert the count span shows 5.',
    ],
  },
  {
    id: "DW_008",
    scenario: "Verify disabled button becomes enabled after delay",
    expected: "dw-submit-btn transitions from disabled to enabled.",
    type: "positive",
    priority: "high",
    steps: [
      'Assert <code>[data-testid="dw-submit-btn"]</code> is disabled on load.',
      'Wait for <code>waitFor({ state: "enabled" })</code>.',
      'Assert the button is now enabled.',
      'Click it and assert the result.',
    ],
  },
  {
    id: "DW_009",
    scenario: "Verify text change is detected via waitForFunction",
    expected: "Status span text changes from 'Idle' to 'Done'.",
    type: "positive",
    priority: "medium",
    steps: [
      'Click the status trigger.',
      'Use <code>waitForFunction</code> until the span text is "Done".',
      'Assert final text value.',
    ],
  },
  {
    id: "DW_010",
    scenario: "Verify simulated fetch result appears after network wait",
    expected: "dw-fetch-result is visible with non-empty text.",
    type: "positive",
    priority: "medium",
    steps: [
      'Click <code>[data-testid="dw-fetch-btn"]</code>.',
      'Wait for <code>[data-testid="dw-fetch-result"]</code> to be visible.',
      'Assert its text is not empty.',
    ],
  },
  {
    id: "DW_011",
    scenario: "Verify race-condition element can be caught within timeout",
    expected: "Element is found before it disappears when timeout is sufficient.",
    type: "positive",
    priority: "medium",
    steps: [
      'Click <code>[data-testid="dw-race-btn"]</code> trigger.',
      'Use <code>waitForSelector(\'[data-testid="dw-race-target"]\', { timeout: 5000 })</code>.',
      'If found, assert it is visible; if not, assert a TimeoutError was thrown.',
    ],
  },
  {
    id: "DW_012",
    scenario: "Verify Cypress timeout override works for slow elements",
    expected: ".should() assertion passes when custom timeout is large enough.",
    type: "positive",
    priority: "medium",
    steps: [
      'Click the trigger that produces a 3-second delay.',
      'Use <code>cy.get(\'[data-testid="dw-delayed-result"]\', { timeout: 5000 }).should(\'be.visible\')</code>.',
      'Assert the element appears.',
    ],
  },
  {
    id: "DW_013",
    scenario: "Verify Selenium WebDriverWait with custom polling interval",
    expected: "Element is found without excessive polling overhead.",
    type: "positive",
    priority: "low",
    steps: [
      'Instantiate <code>new WebDriverWait(driver, Duration.ofSeconds(10)).pollingEvery(Duration.ofMillis(500))</code>.',
      'Wait for <code>visibilityOfElementLocated</code> on the target element.',
      'Assert the element is displayed.',
    ],
  },
  {
    id: "DW_014",
    scenario: "Verify no hard sleep (Thread.sleep / page.waitForTimeout) is used",
    expected: "Tests complete faster using condition-based waits rather than fixed delays.",
    type: "edge",
    priority: "low",
    steps: [
      'Review test code for <code>Thread.sleep</code> or <code>page.waitForTimeout</code>.',
      'Replace any fixed sleeps with <code>waitForSelector</code>, <code>waitForFunction</code>, or <code>ExpectedConditions</code>.',
      'Assert tests still pass and run faster on average.',
    ],
  },
];
