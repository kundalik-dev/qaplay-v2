import type { TestCase } from "@/data/practice-data/types";

export const annotationsTestCases: TestCase[] = [
  {
    id: "ANN_001",
    scenario: "Beta panel hidden when feature flag is OFF",
    expected: "beta-panel is not visible when toggle aria-checked is false",
    type: "positive",
    priority: "high",
    steps: [
      "Navigate to <code>/practice/annotations</code>",
      'Assert <code>[data-testid="toggle-beta-ui"]</code> has <code>aria-checked="false"</code>',
      'Assert <code>[data-testid="beta-panel"]</code> is hidden',
      "Apply <code>test.skip(!isBetaOn, 'Beta UI flag is OFF')</code> in your test",
    ],
  },
  {
    id: "ANN_002",
    scenario: "Beta panel visible after toggling feature flag ON",
    expected: "beta-panel appears with text 'Beta Feature Visible' and enabled action button",
    type: "positive",
    priority: "high",
    steps: [
      "Navigate to <code>/practice/annotations</code>",
      'Click <code>[data-testid="toggle-beta-ui"]</code> to turn it ON',
      'Assert <code>aria-checked="true"</code> on the toggle',
      'Assert <code>[data-testid="beta-panel"]</code> is visible',
      'Assert panel contains text <code>"Beta Feature Visible"</code>',
      'Assert <code>[data-testid="btn-beta-action"]</code> is enabled',
    ],
  },
  {
    id: "ANN_003",
    scenario: "Environment badge updates when switching environments",
    expected: "env-badge changes from 'Staging' to 'Production' after button click",
    type: "positive",
    priority: "medium",
    steps: [
      'Assert <code>[data-testid="env-badge"]</code> contains <code>"Staging"</code>',
      'Click <code>[data-testid="btn-switch-env"]</code>',
      'Assert <code>[data-testid="env-badge"]</code> contains <code>"Production"</code>',
      'Assert <code>aria-label</code> on badge reads <code>"Current environment: Production"</code>',
    ],
  },
  {
    id: "ANN_004",
    scenario: "Loading indicator aria-busy attribute transitions during slow op",
    expected: "aria-busy changes from false → true → false as report generates",
    type: "positive",
    priority: "high",
    steps: [
      "Call <code>test.slow()</code> at the top of the test",
      'Assert <code>[data-testid="loading-indicator"]</code> has <code>aria-busy="false"</code> initially',
      'Click <code>[data-testid="btn-gen-report"]</code>',
      'Assert <code>aria-busy="true"</code> immediately after click',
      'Assert <code>[data-testid="loading-label"]</code> contains <code>"Generate sales report"</code>',
      'Wait for <code>[data-testid="report-result"]</code> to be visible',
      'Assert <code>aria-busy="false"</code> after operation completes',
    ],
  },
  {
    id: "ANN_005",
    scenario: "Report table has exactly 4 rows after generation",
    expected: "report-tbody contains 4 <tr> elements with correct data",
    type: "positive",
    priority: "high",
    steps: [
      "Call <code>test.slow()</code>",
      'Click <code>[data-testid="btn-gen-report"]</code>',
      'Wait for <code>[data-testid="report-result"]</code> to be visible',
      'Assert <code>locator("#report-tbody tr").count()</code> equals <code>4</code>',
      'Assert <code>[data-testid="report-row-0"]</code> contains <code>"January"</code> and <code>"Above target"</code>',
    ],
  },
  {
    id: "ANN_006",
    scenario: "Buggy counter increments by 2 instead of 1 (BUG-101)",
    expected: "Counter shows 6 after 3 clicks — annotation test.fixme() marks it broken",
    type: "negative",
    priority: "high",
    steps: [
      'Assert <code>[data-testid="buggy-counter"]</code> shows <code>"0"</code>',
      'Click <code>[data-testid="btn-buggy-inc"]</code> 3 times',
      'Assert counter shows <code>"6"</code> (not "3")',
      'Annotate: <code>test.fixme(true, "BUG-101: adds 2 per click")</code>',
    ],
    note: "BUG-101: increment adds 2 per click. Decrement is correct.",
  },
  {
    id: "ANN_007",
    scenario: "Counter decrement works correctly (no bug)",
    expected: "Counter decreases by 1 per click with correct floor at 0",
    type: "positive",
    priority: "medium",
    steps: [
      'Click <code>[data-testid="btn-buggy-inc"]</code> once (counter = 2 due to bug)',
      'Click <code>[data-testid="btn-buggy-dec"]</code> once',
      'Assert counter shows <code>"1"</code>',
      'Click <code>[data-testid="btn-buggy-dec"]</code> again',
      'Assert counter shows <code>"0"</code> (floor — does not go negative)',
    ],
  },
  {
    id: "ANN_008",
    scenario: "test.fail() confirms BUG-101 still exists (regression check)",
    expected: "test.fail() annotation inverts result — test 'passes' when assertion fails",
    type: "edge",
    priority: "medium",
    steps: [
      "Add <code>test.fail()</code> annotation at top of test",
      'Click <code>[data-testid="btn-buggy-inc"]</code> 3 times',
      'Assert <code>toHaveText("3")</code> — this assertion FAILS (actual: "6")',
      'test.fail() causes the test to PASS because it expected a failure',
      "If bug is fixed, assertion passes → test.fail() flips → whole test FAILS (remove annotation)",
    ],
    note: "This is a regression guard. When the test starts failing unexpectedly, the bug has been fixed.",
  },
  {
    id: "ANN_009",
    scenario: "Checkout step 1 — cart review and quantity update",
    expected: "Cart table shows both products; total updates on quantity change",
    type: "positive",
    priority: "high",
    steps: [
      'Assert <code>[data-testid="step-tab-1"]</code> has class <code>active</code>',
      'Assert <code>[data-testid="cart-table"]</code> contains <code>"Wireless Headphones"</code> and <code>"USB-C Hub"</code>',
      'Change <code>[data-testid="cart-qty-1"]</code> to <code>2</code>',
      'Assert <code>[data-testid="cart-total"]</code> updates to reflect new quantity',
      'Click <code>[data-testid="btn-to-shipping"]</code>',
    ],
  },
  {
    id: "ANN_010",
    scenario: "Checkout step 2 — fill shipping form and proceed",
    expected: "Step 1 tab shows done class; step 2 panel is visible after form fill",
    type: "positive",
    priority: "high",
    steps: [
      'Complete step 1 and click <code>[data-testid="btn-to-shipping"]</code>',
      'Assert <code>[data-testid="step-tab-1"]</code> has class <code>done</code>',
      'Assert <code>[data-testid="step-panel-2"]</code> is visible',
      'Fill <code>[data-testid="ship-name"]</code>, <code>ship-address</code>, <code>ship-zip</code>',
      'Select <code>"express"</code> in <code>[data-testid="ship-method"]</code>',
      'Click <code>[data-testid="btn-to-payment"]</code>',
    ],
  },
  {
    id: "ANN_011",
    scenario: "Checkout step 3 — place order and confirm",
    expected: "order-confirmation is visible with an ORD- prefixed order ID",
    type: "positive",
    priority: "high",
    steps: [
      'Assert <code>[data-testid="step-panel-3"]</code> is visible',
      'Fill <code>[data-testid="pay-card"]</code> with <code>"4111 1111 1111 1111"</code>',
      'Fill <code>[data-testid="pay-expiry"]</code> with <code>"12/27"</code>',
      'Fill <code>[data-testid="pay-cvv"]</code> with <code>"123"</code>',
      'Click <code>[data-testid="btn-place-order"]</code>',
      'Assert <code>[data-testid="order-confirmation"]</code> is visible',
      'Assert <code>[data-testid="order-id"]</code> text matches <code>/^ORD-/</code>',
    ],
  },
  {
    id: "ANN_012",
    scenario: "Data-driven login — Admin user gets full dashboard",
    expected: "dash-admin is visible; dash-editor and dash-viewer are hidden",
    type: "positive",
    priority: "high",
    steps: [
      'Fill <code>[data-testid="login-email"]</code> with <code>admin@test.com</code>',
      'Fill <code>[data-testid="login-password"]</code> with <code>admin123</code>',
      'Click <code>[data-testid="btn-login"]</code>',
      'Assert <code>[data-testid="login-result"]</code> contains <code>"role: admin"</code>',
      'Assert <code>[data-testid="dash-admin"]</code> is visible',
      'Assert <code>[data-testid="dash-editor"]</code> is hidden',
      'Assert <code>[data-testid="admin-manage-users"]</code> is visible',
    ],
  },
  {
    id: "ANN_013",
    scenario: "Data-driven login — Invalid credentials show error, no dashboard",
    expected: "login-result shows 'Invalid credentials'; role-dashboard is hidden",
    type: "negative",
    priority: "high",
    steps: [
      'Fill <code>[data-testid="login-email"]</code> with <code>bad@test.com</code>',
      'Fill <code>[data-testid="login-password"]</code> with <code>wrong</code>',
      'Click <code>[data-testid="btn-login"]</code>',
      'Assert <code>[data-testid="login-result"]</code> contains <code>"Invalid credentials"</code>',
      'Assert <code>[data-testid="role-dashboard"]</code> is hidden',
    ],
  },
  {
    id: "ANN_014",
    scenario: "Logout restores initial login state for next test iteration",
    expected: "Form fields cleared, dashboard hidden, login button visible again",
    type: "positive",
    priority: "medium",
    steps: [
      "Login as any valid user",
      'Click <code>[data-testid="btn-logout"]</code>',
      'Assert <code>[data-testid="role-dashboard"]</code> is hidden',
      'Assert <code>[data-testid="login-email"]</code> is empty',
      'Assert <code>[data-testid="btn-login"]</code> is visible',
      'Assert <code>[data-testid="btn-logout"]</code> is hidden',
    ],
    note: "Critical for test isolation when using test.each() — each iteration must start clean.",
  },
];
